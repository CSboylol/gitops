apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-3-dev
  namespace: app-3-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-3-dev
  template:
    metadata:
      labels:
        app: app-3-dev
    spec:
      containers:
        - name: nginx
          image: nginx:1.27
          ports:
            - containerPort: 80
