apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-2-dev
  namespace: app-2-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-2-dev
  template:
    metadata:
      labels:
        app: app-2-dev
    spec:
      containers:
        - name: nginx
          image: nginx:1.27
          ports:
            - containerPort: 80
